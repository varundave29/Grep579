We have completed the read-only architecture discovery.

Do NOT modify any code.

Now narrow the investigation specifically to the existing OpenAPI/Swagger surface and job cancellation.

1. Confirm whether the current FastAPI application exposes:
   - /docs
   - /redoc
   - /openapi.json

2. Identify the exact existing Swagger/OpenAPI configuration in the codebase.

3. Identify the exact existing REST endpoint(s) responsible for:
   - starting a job
   - retrieving a job
   - retrieving running jobs
   - retrieving a specific run
   - cancelling a run/job
   - retrieving run history
   - retrieving job output/logs

4. For the cancellation functionality, trace the complete implementation:
   frontend button/action
      → API endpoint
      → route handler
      → executor/job manager
      → subprocess/task cancellation
      → state update
      → frontend result

5. Determine whether cancellation is already fully implemented and usable, or whether there are gaps.

6. Determine whether the existing APIs are already represented correctly in the generated OpenAPI schema.

7. Identify only the minimum documentation improvements that would make the existing APIs easier to understand and troubleshoot through Swagger.

Do not propose a new API architecture.

Do not create new endpoints unless you discover that an existing required capability genuinely has no API.

Do not change any code.

At the end, give me ONLY:

A. Current Swagger status
B. Exact URLs to access it locally
C. Existing job-related API endpoints
D. Existing cancellation implementation
E. What we can test immediately through Swagger
F. Any genuine gaps that need implementation
G. The smallest possible next implementation step
