Continue refining the dashboard UI based on the existing implementation and the previous UI changes.

IMPORTANT:
- This is still primarily a UI/layout refinement.
- Preserve all existing backend APIs, calculations, insight generation, tenant utilization calculations, routing, navigation, job execution, and data-fetching logic.
- Do not invent or hardcode insight data.
- Inspect the existing data model/components to understand what each displayed value actually represents before changing its presentation.
- Preserve existing functionality while making the UI substantially more compact, readable, and information-dense.
- Do not make unrelated dashboard changes.

I want the following additional refinements.

==================================================
1. TENANT UTILIZATION CPU/MEMORY ROW LAYOUT
==================================================

For each tenant/application in the Tenant Utilization widget, restructure the CPU and Memory utilization rows.

The intended visual relationship is:

CPU (limit): 71%                         9.20 cores free
[=========================----------]

Memory (reserved): 23%                  196.22 GB free
[========----------------------------]

The exact values must come from the existing data.

Important layout behavior:

- The CPU/Memory label and utilization percentage should be left aligned.
- The free capacity should be right aligned.
- The utilization bar should visually sit underneath or integrate with this row.
- The filled portion of the bar represents the utilization percentage.
- The empty portion of the bar represents the remaining/free capacity visually.
- The percentage should therefore correspond to the actual fill level.
- The free value on the right should make the meaning of the empty portion explicit.

For example:

CPU (limit): 71%                         9.20 cores free
[███████████████████████░░░░░░░░░]

Memory (reserved): 23%                   196.22 GB free
[███████░░░░░░░░░░░░░░░░░░░░░░]

Use the existing dashboard color system for the utilization bar.

Do not add a second redundant black information box.

The existing hover interaction must remain available and should continue to provide the detailed breakdown.

Do not change the underlying calculations.

==================================================
2. DASHBOARD/PAGE-LEVEL VERTICAL OVERFLOW
==================================================

The Kubernetes Operational Analytics section is currently extending beyond the visible dashboard area, causing the lower insight cards to become inaccessible.

Fix the overall page layout so that:

- The dashboard attempts to fit all content naturally within the available viewport.
- Do not force unnecessary vertical scrolling when the content can fit.
- Use the space gained from compacting Tenant Utilization and Kubernetes Operational Analytics.
- If the amount of content genuinely exceeds the available viewport, provide a vertical scroll container.

IMPORTANT:
The scrollbar should only appear when content actually overflows.

Do NOT permanently display a scrollbar.

Use a modern minimal scrollbar:
- No large scrollbar track/background.
- Subtle thumb.
- Compact width.
- Visually consistent with the application's existing dark/light themes.
- It should feel like a modern application scrollbar rather than a browser-default large scrollbar.

The scrolling behavior should allow the user to reach every insight card when there are many cards or when additional applications/insights are onboarded.

Do not create nested scrolling containers unnecessarily.

Prefer one clearly defined dashboard/content scroll area rather than multiple independent scrollbars.

==================================================
3. KUBERNETES OPERATIONAL ANALYTICS HEADER
==================================================

The current analytics widget uses too much vertical space.

Current structure is approximately:

Kubernetes Operational Analytics
Evidence-backed insights with analytical path, statistical baselines and uncertainty context

[ ENGINE ]
ready

[ ACTIVE INSIGHTS ]
5

[ MODEL ]
Rules + Stats + Path

Then the insight cards.

I do NOT want the Engine / Active Insights / Model information occupying a separate second row.

Move these into the main header row.

The desired conceptual structure is:

Kubernetes Operational Analytics
Evidence-backed insights with analytical path, statistical baselines and uncertainty context

                                    Engine: Ready
                                    Active Insights: 5
                                    Model: Rules + Stats + Path

The exact alignment can be adapted to the available width.

The important requirement is:

ONE COMPACT HEADER ROW

containing:

1. Kubernetes Operational Analytics
2. Engine status
3. Active Insights count
4. Model information

Do not create three large metric boxes underneath the title.

==================================================
4. ENGINE STATUS
==================================================

The Engine status should be presented as a compact status label/badge.

For example:

Engine: Ready

The status text should be presentation-friendly.

Instead of:

ready

display:

Ready

Capitalization should be correct for user-facing presentation.

Apply semantic color coding based on the actual engine status.

For example:

Ready      -> green
Warning    -> appropriate warning color
Error      -> appropriate error color
Unavailable/Not Ready -> appropriate neutral/error state

Do NOT hardcode Ready as the only state.

Use the actual status returned by the existing implementation.

The color should change according to the existing status value.

Keep the status visually subtle and compact. Do not create a large colored card.

==================================================
5. ACTIVE INSIGHTS
==================================================

Display the existing active insight count compactly in the header.

For example:

Active Insights: 5

The count must come from the existing data.

Do not hardcode the number.

If the current implementation already has additional insight metadata that is useful, inspect it and determine whether it can be represented compactly.

Do not introduce additional statistics simply for visual decoration.

==================================================
6. MODEL
==================================================

Display the existing model information compactly in the header.

For example:

Model: Rules + Stats + Path

Use whatever model/path information is actually provided by the current implementation.

Do not invent additional models.

If there are multiple model-related values in the existing data, inspect them and choose a compact representation that accurately reflects the current implementation.

The goal is simply to expose the existing model information without consuming a full row of dashboard space.

==================================================
7. ANALYTICS INSIGHT CARDS: COMPACT REDESIGN
==================================================

The current insight cards are too large both horizontally and vertically.

Redesign the card layout to be more compact and information-dense.

The current cards contain things such as:

- Severity
- Insight/service name
- Insight description
- Confidence
- Confidence level
- Timestamp
- Status
- Evidence button
- Open App button
- A horizontal bar

The information itself is useful, but the current spacing/padding makes each card unnecessarily tall.

Reduce:
- Card padding
- Excessive vertical gaps
- Oversized headers
- Excessive button spacing
- Empty space between metadata and description
- Excessive height of the progress/confidence area

Keep the cards visually clean and easy to scan.

==================================================
8. INSIGHT CARD GRID
==================================================

Use the available horizontal space more effectively.

The cards should be able to display more insights within the viewport.

Prefer a responsive grid that adapts to the available width.

For the current desktop dashboard, aim for a compact multi-column layout similar to the existing 3-column arrangement, but reduce card width/height where possible.

Do not force a fixed width that breaks when the viewport changes.

The layout should gracefully adapt when:
- There are fewer insights.
- There are more insights.
- The browser/window width changes.
- The dashboard is viewed at different supported resolutions.

Do not create horizontal scrolling.

==================================================
9. INSIGHT CARD CONTENT HIERARCHY
==================================================

Improve the visual hierarchy.

The most important information should be immediately visible:

1. Severity
2. Insight/service identifier
3. Main insight statement
4. Confidence/quality indicator
5. Timestamp/status
6. Actions

For example, the card should visually communicate something like:

[CRITICAL]   db-svc-odm-processor

Availability rate has degraded relative to historical baseline.

Confidence: 68%     Medium
24 Sep 2026, 02:06 IST                         ACTIVE

[ Evidence ] [ Open App ]

Do not literally hardcode this structure if the existing data differs. Adapt it to the actual data.

==================================================
10. CRITICAL / WARNING / INFORMATION VISUAL STATES
==================================================

Preserve the existing severity system.

Critical insights should remain visually identifiable.

However, avoid making every element of the card heavily colored.

Use color primarily for:
- Severity
- Status
- Important indicators

Keep the rest of the card neutral so that the dashboard does not become visually noisy.

The current screenshot has a lot of pink/red/cyan competing for attention.

Tone this down while retaining clear severity differentiation.

==================================================
11. THE HORIZONTAL BAR / CONFIDENCE VISUALIZATION
==================================================

IMPORTANT: inspect the existing implementation before changing this.

There is currently a horizontal cyan bar on each insight card.

I do not want a decorative bar whose meaning is unclear.

Determine exactly what this bar currently represents.

If it represents confidence:
- Make that explicit.
- The fill percentage must correspond to the actual confidence value.
- Example: Confidence 68% should visually fill approximately 68% of the bar.
- Consider labeling it clearly as Confidence or otherwise making the relationship obvious.

If it represents another metric:
- Preserve the underlying metric.
- Make the metric understandable through an appropriate label/value.
- Ensure the visual fill corresponds to the actual metric.

If it is purely decorative and does not represent data:
- Remove it rather than keeping a meaningless visual element.

Do NOT change the underlying insight calculation.

The final UI should make it immediately obvious why the bar exists.

==================================================
12. CONFIDENCE DISPLAY
==================================================

If the existing insight data contains confidence percentage and confidence category such as:

Confidence 68%
Medium

keep both pieces of information, but make them compact.

Avoid displaying the same information multiple times.

For example:

Confidence 68%   Medium

followed by a visually proportional confidence bar is sufficient.

==================================================
13. BUTTONS / ACTIONS
==================================================

Keep the existing:

- Evidence
- Open App

functionality.

Do not remove these actions.

Make the buttons more compact if necessary.

They should not consume a disproportionate amount of vertical space.

Preserve their existing click behavior.

==================================================
14. TIMESTAMP AND ACTIVE STATUS
==================================================

Keep timestamp and active status information.

Make it compact and secondary to the insight itself.

For example:

24 Sep 2026, 02:06 IST    ACTIVE

Use the actual existing values.

Do not hardcode these.

==================================================
15. RESPONSIVE BEHAVIOR WHEN MORE DATA EXISTS
==================================================

The dashboard should look compact with the current amount of data but remain scalable.

If more applications are onboarded:
- Tenant Utilization should expand naturally.
- Pod Count Summary should accommodate additional applications.
- Analytics should accommodate additional insight cards.
- The dashboard should not become visually broken.
- Vertical scrolling should appear only when the content genuinely exceeds the available dashboard viewport.

Do not solve overflow by simply hiding content.

All insights must remain accessible.

==================================================
16. OVERALL VISUAL OBJECTIVE
==================================================

The final dashboard should feel like a compact operational console rather than a collection of oversized cards.

The hierarchy should be approximately:

HEADER
DevOps Automation Console

TOP AREA
Tenant Utilization                 Pod Count Summary

ANALYTICS
Kubernetes Operational Analytics   Engine: Ready
                                  Active Insights: 5
                                  Model: Rules + Stats + Path

INSIGHT GRID
[compact insight] [compact insight] [compact insight]
[compact insight] [compact insight] [compact insight]
...

Use the available viewport intelligently.

Do not sacrifice readability for density.

==================================================
17. VALIDATION
==================================================

Before completing the work, verify:

- CPU utilization percentage is left aligned.
- CPU free cores are right aligned.
- Memory utilization percentage is left aligned.
- Memory free memory is right aligned.
- Utilization bar fill corresponds to actual utilization.
- Existing hover breakdown still works.
- Tenant cards are shorter than before.
- Analytics header no longer consumes a separate metric row.
- Engine status is shown in the header.
- Engine status is correctly capitalized.
- Engine status color changes according to actual status.
- Active Insights count is shown in the header.
- Model information is shown in the header.
- Insight cards are significantly more compact.
- Multiple insight cards fit in the viewport where possible.
- The confidence/metric bar has an understandable meaning.
- If the bar represents confidence, its fill corresponds to the actual confidence percentage.
- Evidence button still works.
- Open App button still works.
- Existing insight data remains intact.
- No backend/API/job logic was changed.
- No horizontal scrolling was introduced.
- Vertical scrolling appears only when the content actually overflows.
- The scrollbar is modern, minimal, and has no large visible background track.
- The dashboard remains usable with substantially more applications/insights.
- Test both dark and light themes if both are supported.

After implementation, provide a concise summary of:
1. Files/components changed.
2. Tenant Utilization changes.
3. Analytics header changes.
4. Insight-card redesign.
5. Scroll/overflow behavior.
6. Confirmation that existing functionality and data logic were preserved.
