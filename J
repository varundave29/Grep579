This is a NEW debugging and implementation task in an existing repository.

Do not rely on previous coding-agent conversations or previous claims that these issues were fixed.

Inspect the current implementation and reproduce the problems before changing code.

There are three related report/live-monitoring issues to investigate and fix.

==================================================
1. LIVE MONITORING IS STILL BROKEN
==================================================

When the user selects Live Monitoring and clicks Start Live Monitoring, the UI immediately transitions into what appears to be the completed-run/report state.

The screen shows things such as:

- Download Report
- Close
- "Waiting for the first metrics sample..."

The live monitoring session does not remain in the active live-monitoring state.

A previous agent claimed this was fixed, restarted the application, and the exact same behavior still occurred.

Therefore, do NOT assume the previous fix was correct.

Trace the complete lifecycle:

Start Live Monitoring
→ backend run/session creation
→ live job starts
→ WebSocket connection
→ first metrics sample
→ continuous metrics samples
→ user Stop
→ completed state

Determine exactly why the UI is entering the completed state immediately after Start.

Inspect:

- frontend state transitions
- Start handler
- backend run response
- job status handling
- WebSocket creation
- WebSocket connection lifecycle
- WebSocket message handling
- cleanup logic
- Snapshot/Live mode branching
- completed/finished state handling

Do not simply patch symptoms.

Verify whether the backend process is actually still running when the frontend changes state.

Preserve the existing WebSocket architecture.

Do not create a second WebSocket implementation.

The live session must remain active until:

- the user explicitly stops it, or
- the backend role-based timeout terminates it.

Preserve the existing:
- per-user session isolation
- active session limit of 10
- developer 10-minute timeout
- tester/unlimited live monitoring behavior

==================================================
2. DOWNLOAD REPORT IS BROKEN
==================================================

The generated report file exists correctly in backend storage/archive.

However, clicking Download Report does not actually download the file.

The browser appears to receive/show something such as:

report.json

but no usable report file is downloaded.

This previously worked.

Trace the complete path:

Download Report button
→ frontend handler
→ HTTP/API request
→ backend download route
→ report lookup
→ filesystem path
→ HTTP file response
→ Content-Type
→ Content-Disposition
→ browser download

Determine the actual root cause.

Do not regenerate the report as a workaround.

Serve the existing generated artifact.

Verify that the browser receives the actual file contents and not an empty/incorrect JSON response.

==================================================
3. REPORT FILE NAMING
==================================================

IMPORTANT:

DO NOT hardcode, reconstruct, infer, or manually map the application ID.

The existing report metadata/meta JSON already contains an application ID placeholder/value.

Inspect the existing metadata structure and identify the authoritative application ID field.

That existing application ID value must be reused as the source of truth for report naming.

Do not introduce another application-ID mapping.

Do not derive the ID from the application display name.

Do not hardcode application IDs.

The report filename must follow this structure:

<application_id>_<namespace>_<run_id>.html

Where:

application_id
→ taken directly from the existing report metadata/application_id value.

namespace
→ the exact namespace configured/selected for this run.

run_id
→ the actual run/build ID associated with this report.

Example structure only:

<existing application_id>_<exact namespace>_<actual run id>.html

The example above is illustrative only.

Do NOT hardcode any example values.

The same naming convention must be used consistently:

1. When the backend generates/stores the report artifact.
2. In report metadata if the filename is represented there.
3. When the backend serves the report.
4. In the Content-Disposition filename.
5. When the frontend downloads the report.

There must not be one filename on disk and another filename presented to the user unless the architecture explicitly requires it.

Use the existing metadata/application_id placeholder as the authoritative source.

==================================================
4. NAMESPACE MUST REMAIN EXACT
==================================================

There is currently logic that attempts to convert the Kubernetes namespace into a human-readable/beautified environment name.

For example, technical namespace names may contain:

- prefixes
- application identifiers
- environment identifiers
- multiple underscore-separated components
- other application-specific naming patterns

The current beautification logic works for some namespaces but fails for others because namespace naming conventions are more complicated.

REMOVE the dependency on this beautification logic for the report.

The report must display the exact namespace configured for the application.

If the configured namespace is:

<actual technical namespace>

then the report header must display:

<actual technical namespace>

Do not strip prefixes.

Do not extract only the environment name.

Do not attempt to parse the namespace.

Do not transform underscores.

Do not shorten it.

Do not replace it with a friendly alias.

The namespace displayed in the generated report must exactly match the namespace selected/configured for the run.

==================================================
5. REPORT HEADER
==================================================

The generated HTML report already has a header/summary section containing information such as:

- timestamp
- namespace
- other run information

The namespace shown there must now be the exact configured namespace.

Remove/bypass the human-readable namespace transformation for this report field.

Use the same authoritative namespace value used for the actual Kubernetes collection.

There should be no possibility of:

Kubernetes collection namespace:
actual-technical-namespace

but report header namespace:
dev

The report header must show:

actual-technical-namespace

exactly.

==================================================
6. DO NOT CONFUSE DISPLAY LABELS WITH REPORT DATA
==================================================

The UI may have human-friendly labels elsewhere if the existing application requires them.

Do NOT globally remove every namespace display transformation from the application.

The requirement is specifically that the generated report and its filename use the exact namespace value.

Therefore distinguish:

UI-friendly namespace label
versus
authoritative namespace identifier.

For the report:

authoritative namespace identifier must be used.

==================================================
7. SINGLE SOURCE OF TRUTH
==================================================

The report should use authoritative values already present in the existing run/report metadata rather than reconstructing them from unrelated UI state.

For the filename and report header:

Application ID:
→ existing metadata application_id

Namespace:
→ exact namespace associated with this run

Run ID:
→ actual run/build ID associated with this run

Do not duplicate or independently calculate these values if the existing backend metadata already contains them.

Inspect the metadata JSON and existing report-generation code first.

==================================================
8. DO NOT BREAK EXISTING FUNCTIONALITY
==================================================

Preserve:

- Snapshot mode
- Live Monitoring
- per-user session isolation
- active metrics session limit
- developer timeout
- tester/unlimited monitoring
- existing WebSocket implementation
- existing metrics collector
- Insight Engine
- existing 20-run history
- triggered_by/user information
- shared dashboard cache separation
- authentication/session handling
- existing report generation content

Do not redesign unrelated jobs.

==================================================
9. INVESTIGATION FIRST
==================================================

Before making changes, identify:

1. The exact frontend state transition causing Live Monitoring to appear completed.
2. Whether the backend live job is still running at that point.
3. Whether the WebSocket is established.
4. The exact response from the Start Live API.
5. The exact condition that causes the completed/report UI to render.
6. The exact frontend/backend path used by Download Report.
7. Why the browser receives an empty/incorrect download.
8. Where the report artifact is generated.
9. Where the report filename is currently generated.
10. The existing metadata JSON structure.
11. The exact authoritative application_id field in that metadata.
12. The exact namespace field associated with the run.
13. Where namespace beautification currently occurs.
14. Where the report header gets its namespace value.
15. Where Content-Disposition gets its filename.

Only then implement the fixes.

==================================================
10. VERIFICATION
==================================================

After implementation, verify:

LIVE:

1. Select Live Monitoring.
2. Click Start.
3. UI remains in live state.
4. WebSocket connects.
5. First metrics sample appears.
6. Subsequent samples continue arriving.
7. Stop transitions to completed state.
8. Developer timeout still works.
9. Session slot is released correctly.

REPORT DOWNLOAD:

1. Run a metrics job that generates a report.
2. Confirm the report exists on backend storage.
3. Click Download Report.
4. Confirm a real file is downloaded.
5. Open the downloaded HTML file.
6. Confirm the report contains the expected data.
7. Confirm the filename follows:

   <application_id>_<namespace>_<run_id>.html

8. Confirm application_id came directly from the existing metadata application_id field.
9. Confirm no application ID was hardcoded or reconstructed.
10. Confirm the namespace in the filename exactly matches the configured namespace.
11. Confirm the namespace in the report header exactly matches the configured namespace.
12. Test at least two namespaces with complicated technical names.
13. Confirm there is NO beautification, stripping, parsing, or environment-name conversion.
14. Confirm different runs generate distinct filenames.

IMPORTANT:

Do not declare the issue fixed merely because the UI stops showing an error.

Actually verify the live WebSocket receives continuous data and the downloaded HTML file exists locally and contains the expected report.

Keep the changes narrowly scoped.

Do not perform unnecessary refactoring.
