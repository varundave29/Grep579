Implement the Global UI Shell / Navigation Overhaul shown in the latest UI mockup and the current application screenshots.

IMPORTANT: This is a UI architecture/layout refactor. Do NOT change, rewrite, or simplify existing business logic, API calls, data fetching, job execution, log streaming, state management, routing behavior, or page functionality. Preserve all existing functionality exactly as-is.

Use the latest generated UI mockup in this conversation as the visual direction.

OBJECTIVE

Replace the current top-level tab navigation:

Dashboard | Jobs | Console | Log Streamer

with one centralized, reusable application sidebar.

The sidebar must be part of the main application shell, NOT implemented separately inside individual pages.

GLOBAL SIDEBAR

• Expanded by default.
• Collapsible.
• Expanded state shows:
  - Dashboard
  - Jobs
  - Console
    - Pod Status
    - Pod Metrics
    - Tenant Utilization
    - ADO Master Report
  - Log Streamer
• Collapsed state shows only the relevant icons for each top-level module.
• Console's children must remain available when Console is expanded/selected.
• The navigation architecture must be data-driven/reusable so future modules and nested submodules/pages can be added without creating another navigation implementation.
• Preserve the existing Console navigation functionality, but move it into the global application sidebar.
• Remove the current Console-specific/internal sidebar/hamburger once its navigation is represented by the global sidebar.

APPLICATION SHELL

Create/use a common application shell containing:

• Header
• Global navigation sidebar
• Main content area
• Global bottom-right info/help button

All four modules must use this same shell.

Do not duplicate navigation logic between pages.

DASHBOARD

The Dashboard should get the maximum available content area after introducing the sidebar.

Dashboard-specific refresh controls must remain Dashboard-specific.

IMPORTANT:
• Refresh must NOT become a global application control.
• Refresh belongs to the Dashboard page only.
• Do not add the Dashboard refresh control to Console, Jobs, or Log Streamer.
• If the current Console/header contains a generic refresh control, remove it as part of this restructuring unless it is required by an existing Console-specific operation.

Dashboard header layout:

Right-align the Dashboard refresh/status area.

Use a compact two-row structure:

Row 1:
[ Refresh ]

Row 2:
[ Auto-refresh status/details ] [ Last updated timestamp ]

Keep these compact and right-aligned so the main dashboard content gets maximum horizontal space.

Do not put "Refresh", "Auto-refresh", and the timestamp into one long horizontal line.

DASHBOARD CONTENT

Do not redesign the existing Dashboard widgets or change their data/logic.

Only adjust their layout/container sizing as necessary so they can use the additional horizontal space created by the new shell.

CONSOLE

Use the global sidebar.

Console-specific navigation should appear as nested items under Console.

Do not create a second sidebar/hamburger inside Console.

Do not add Dashboard's refresh control to Console.

Preserve all existing Console functionality, filters, data, pod status logic, namespace handling, etc.

JOBS

Use the global sidebar and common application shell.

Do not unnecessarily redesign the Jobs page itself. Preserve its existing functionality and layout, simply give it the new available content area.

LOG STREAMER

Use the global sidebar and common application shell.

Do not redesign the Log Streamer functionality.

Important layout requirement:
• The application should normally fit within the viewport without an outer/page-level scrollbar.
• Do not allow the entire application/page to scroll vertically during normal desktop use.
• Use the available viewport height efficiently.
• If the actual log content exceeds the available area, scrolling should belong to the log/content region itself, not the entire application shell.
• If the browser/window becomes smaller, use responsive scaling/layout and eventually allow appropriate internal scrolling rather than breaking the UI.
• Do not force elements into an unusably small size just to eliminate scrolling.

RESPONSIVE BEHAVIOR

Desktop is the primary target.

Expanded sidebar is the default.

Collapsed sidebar:
• Show only icons.
• Preserve tooltips/accessibility so the meaning of icons is clear.
• Main content should automatically reclaim the sidebar width.

When the viewport becomes smaller:
• Maintain a sensible minimum layout width.
• Avoid overlapping/cut-off controls.
• Prefer responsive reflow/scaling and internal content scrolling where appropriate.
• Do not introduce unnecessary page-level scrolling.

GLOBAL INFO BUTTON

Move the existing information/help button to a fixed bottom-right position as part of the global application shell rather than positioning it relative to an individual page.

VISUAL DIRECTION

Use the latest UI mockup as the visual reference:

• Dark DevOps console aesthetic
• Expanded left navigation
• Clean compact header
• Maximum space for actual data
• Consistent rounded panels/cards
• Existing visual language should be preserved rather than replaced unnecessarily
• Maintain existing status colors and meaning
• Avoid excessive empty space
• Avoid introducing decorative UI that does not serve a function

ARCHITECTURAL REQUIREMENTS

Before modifying code:

1. Inspect the existing routing/navigation structure.
2. Identify where the current top-level navigation is implemented.
3. Identify the current Console-specific navigation.
4. Identify the Dashboard refresh/auto-refresh implementation.
5. Identify any page-level height/overflow/scroll handling.
6. Reuse existing components/styles where practical.

Then implement the shared application shell.

Do not duplicate page logic just to fit the new shell.

Do not modify backend/API contracts.

Do not modify job execution logic.

Do not modify log parsing/streaming logic.

Do not modify dashboard data calculations.

Do not modify Console data-fetching logic.

Do not modify existing routes unless required to support the centralized navigation.

VALIDATION

After implementation:

• Verify all four top-level modules are reachable from the global sidebar.
• Verify Console nested navigation works.
• Verify sidebar collapse/expand works.
• Verify active module/submodule highlighting works.
• Verify Dashboard refresh still works.
• Verify Dashboard auto-refresh still works.
• Verify Dashboard timestamp still works.
• Verify Dashboard refresh controls are NOT shown globally.
• Verify Console does not get Dashboard's refresh control.
• Verify Jobs functionality is unchanged.
• Verify Log Streamer functionality and streaming behavior are unchanged.
• Verify the application normally fits the viewport without an outer page scrollbar.
• Verify internal log/content scrolling still works where required.
• Check desktop and a smaller viewport before finishing.

Most important constraint:
FUNCTIONAL BEHAVIOR MUST REMAIN UNCHANGED. This task is primarily a shared UI shell/navigation/layout refactor, not a functional rewrite.

Before finishing, provide a concise summary of:
1. Files/components changed.
2. New shared shell/navigation structure.
3. How nested navigation is represented.
4. How viewport/overflow behavior was handled.
5. Any functional behavior intentionally left untouched.
