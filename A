Analyze and fix the Kubernetes Log Streamer issue with multi-container pods.

Currently, the container dropdown correctly detects and displays the main container and sidecars, and the main container logs load correctly. However, when switching to a sidecar container, its logs do not load automatically. Refreshing also does not resolve it.

Investigate the existing implementation end-to-end and identify why the selected container is not being used correctly for log retrieval.

Fix it so that:

- Main container logs continue working exactly as they do now.
- Selecting a sidecar automatically loads that sidecar's logs without requiring Refresh or browser refresh.
- Switching back to the main container automatically loads the main container logs.
- Switching between multiple sidecars loads the correct container's logs.
- The actual Kubernetes log request must use the selected container and must not silently fall back to the default container.
- Existing single-container and multi-container behavior must not regress.
- Do not change unrelated functionality or architecture.

There are already tests for the log streamer. Update/add the relevant test coverage for this fix, particularly verifying that the selected container is propagated through to log retrieval and that changing the container automatically reloads the correct logs.

Run the relevant tests and validate the fix end-to-end. If you find a better or more appropriate implementation based on the existing architecture, use it rather than blindly following the above implementation details.

At the end, briefly report the root cause, files changed, tests run, and results.
