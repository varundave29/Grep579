Audit the existing logviewer_rules.yml, rule engine, analytics.py automatic classification, sessions.py, and KubernetesLogViewer.tsx. Document exactly how configured rule badges and automatic insight badges are generated, merged, transmitted, and rendered. Identify the current data model/API/WebSocket payload for a detected badge. Do not modify code. Also determine whether multiple rules can match one log entry and how duplicate/conflicting badges are handled. Finally, recommend the minimum schema changes required to evolve logviewer_rules.yml into a generic application/vendor log-definition framework supporting human-readable labels, categories, descriptions, severity, and occurrence-specific context, while preserving the existing generic detection behavior.
