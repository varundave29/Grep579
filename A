Update the existing Node.js script to support sidecar images without changing the existing `images.yaml` format.

Add a mapping in the script like:

const sidecarTargetMap = {
  "<SIDECAR_IMAGE_NAME>": ["<TARGET_SERVICE_NAME>"]
};

When an image matches this mapping, update its image, digest, and version in the `sidecar` section of the target service's Helm values file instead of treating it as a standalone service.

Support multiple target services for the same sidecar.

Keep all existing behavior unchanged for normal images. Do not scan all values files to discover the mapping. Add/update tests for normal images and sidecar images.
