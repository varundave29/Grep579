Implement the remaining Deployment Manager access/RBAC and Deployment Details improvements. Do not work on the 3D topology in this task. Keep the existing architecture and patterns wherever possible; inspect the current implementation first and make targeted changes.

DEPLOYMENT DETAILS

1. Render the existing DeploymentSummary.conditions data on the Deployment Details page.
   - Show Available / Progressing conditions when present.
   - Include status, reason and message where available.
   - Hide the section when there are no conditions.

2. Show CPU and memory requests/limits for every container in every pod, including sidecars.
   - Do not aggregate these values.
   - Show each container independently.
   - Clearly distinguish Request and Limit.

3. Show current CPU and memory usage as a point-in-time snapshot when Deployment Details is loaded/refreshed.
   - Do not introduce continuous polling/live streaming.
   - Reuse the existing pod metrics infrastructure where practical.
   - Show the sample timestamp.
   - If metrics are unavailable, show an explicit unavailable state rather than 0 or fabricated data.

4. Add rollout/revision history where the Kubernetes data is available.
   - Display previous/current revisions and relevant image/version information where available.
   - Do not implement rollback actions in this task.

5. Do not implement HPA support. It is not used in our environment.

RBAC UI

6. Remove "RBAC" and "Audit" from the breadcrumbs.
   - They are already local tabs/navigation alongside Overview.
   - Breadcrumbs should represent the application/page hierarchy only.

7. The RBAC page must be editable for users who are allowed to administer this application-level access.
   - Do not hard-code the current read-only state.
   - The UI is the management interface, but the backend remains the source of truth and must enforce the permissions.

8. Show only centrally onboarded Developer/Tester users in the editable RBAC list.
   - Admin and DevOps users should not be managed through this table because their existing role/application access already gives them the appropriate Deployment Manager access.
   - Preserve the existing Admin/DevOps authorization behavior.

9. Deployment Manager access should be an explicit editable permission for Developer/Tester users.
   - Granting Deployment Manager access gives the user read/view access to Deployment Manager resources across the application/environment as currently defined.
   - It does NOT by itself grant operational mutation access.

10. Namespace access is the operational permission.
    - Granting a namespace to a Developer/Tester gives them the existing Deployment Manager operational actions for that namespace, such as restart, start/stop, scale, etc.
    - Namespace permissions must remain scoped to the current application.
    - Do not create a global namespace permission that leaks across applications.

11. Keep the main RBAC table readable. Do not create an unnecessarily large user × namespace checkbox matrix.
    - Use an expandable/edit interaction or equivalent clean UI for selecting namespaces.
    - Show the user's current granted namespaces clearly.

12. Persist RBAC changes through the backend and enforce them server-side.
    - Do not rely on frontend-only permission checks.
    - Follow the existing authentication/authorization and database patterns.
    - Add/update audit logging for RBAC changes if the existing Audit infrastructure supports it.

IMPORTANT: REMOVE DEPLOYMENT MANAGER FROM CENTRAL JOB-LEVEL RBAC

13. Deployment Manager is currently registered as a Job in the central Admin Console/User Accounts RBAC system.
    - Remove Deployment Manager from that central Job Access list/permission model.
    - A user should NOT receive Deployment Manager access through the generic central Job Access mechanism.
    - Deployment Manager access must be managed exclusively through the application-level RBAC UI described above.
    - Remove the Job registration/permission entry and any related central-RBAC handling that would cause Deployment Manager to appear as an assignable job.
    - Make sure existing users cannot accidentally retain access through the old job-level path.
    - Do not remove the actual Deployment Manager job/functionality from the console. Only remove its generic central Job Access/RBAC integration.
    - Check both backend authorization logic and the Admin Console/User Accounts UI so there is no duplicate access path or confusing leftover configuration.

ARCHITECTURE / SAFETY

14. Do not redesign the whole authorization system.
15. Inspect the existing implementation first and reuse existing RBAC, authentication, audit, metrics and Kubernetes data plumbing wherever possible.
16. Preserve existing Admin and DevOps behavior.
17. Preserve existing Deployment Manager functionality.
18. Do not add HPA.
19. Do not add continuous CPU/memory polling.
20. Do not touch the 3D topology, Bloom/postprocessing or topology rendering in this task.

VALIDATION

After implementation:
- Run the existing tests.
- Add/update tests for the changed RBAC and Deployment Details behavior.
- Verify Developer/Tester access through the new application-level RBAC path.
- Verify namespace-level operational authorization.
- Verify Admin/DevOps behavior remains unchanged.
- Verify Deployment Manager no longer appears as an assignable Job in the central Admin Console/User Accounts RBAC.
- Verify the old job-level permission cannot bypass the new application-level RBAC.
- Verify Deployment Details correctly renders conditions, per-container resources, point-in-time metrics and rollout history where available.
- Run the normal build/lint/type checks and fix any regressions.

Do not stop at documenting blockers or suggestions. Implement the changes, validate them, and summarize exactly what was changed and any genuinely remaining blockers.
