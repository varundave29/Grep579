Implement the 3D Topology view as a finished production-quality interactive 3D scene. Do not stop at the basic 3D skeleton.

Use the existing project architecture and current React/Three.js stack. Inspect the existing topology implementation first and improve it rather than creating a disconnected prototype.

Target visual:

- Full dark navy/black 3D canvas.
- Large central glowing cyan/blue geodesic sphere/icosphere representing the namespace/cluster.
- Sphere must have a translucent/emissive surface plus a bright triangular wireframe/geodesic structure.
- Add subtle internal glow and animated energy so it feels alive.
- Display namespace name and deployment/pod totals around the central object.
- Arrange deployment nodes radially around the central sphere in 3D space with proper depth/perspective.
- Each deployment must be a real 3D cuboid with depth, lighting, emissive edges and a status-colored appearance.
- Deployment label and replica status must remain readable and visually attached to the 3D node.
- Connect every deployment to the central sphere using glowing 3D lines/beams.
- Add subtle animated particles travelling along the connections.
- Status colors:
  Running = green/cyan
  Not Ready = red
  Updating = blue
  Stopped = grey
- Running nodes should have the strongest healthy glow.
- Not Ready nodes should visibly stand out in red/orange.
- Keep the scene visually close to a futuristic Kubernetes infrastructure topology, not a generic Three.js demo.

Interaction:
- Orbit camera.
- Smooth zoom.
- Reset/recenter camera.
- Hover highlights a deployment and its connection.
- Click selects a deployment.
- Selected deployment should have a stronger glow/highlight.
- Preserve existing deployment details/action behavior.
- Camera should initially frame the entire topology cleanly without clipping nodes.
- Topology must resize correctly with the browser window.

Visual quality:
- Proper 3D lighting.
- Ambient/environment lighting.
- Bloom/glow where supported by the current stack.
- Depth and perspective.
- Smooth animations.
- Avoid excessive glow that makes labels unreadable.
- Avoid overlapping deployment labels.
- Do not use flat 2D cards as substitutes for the 3D nodes.
- Do not use a static image/background to fake the topology.

Use the real deployment data already available in the application. Do not hardcode the example deployment names/counts from the reference.

Most importantly, implement the COMPLETE visual scene, not just the underlying nodes/edges/skeleton.

After implementation:
1. Run the application.
2. Open the topology view.
3. Inspect the actual rendered result.
4. Fix camera framing, node spacing, label readability, lighting, colors, connections and visual hierarchy.
5. Iterate until the result looks like a finished 3D topology visualization rather than a technical prototype.
6. Update/add the relevant tests for the changed topology behavior.
