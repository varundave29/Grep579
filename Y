Investigate the application’s current URL routing and deep-linking capabilities. Do not modify any code.

Check whether the application already has a generic mechanism that maps modules, sub-modules, dashboards, jobs, job executions, job histories, reports, console views, or other selected resources to browser URLs.

Specifically determine:

* What router/routing mechanism is currently used.
* Which application states currently update the browser URL, if any.
* Whether job IDs, execution IDs, history IDs, or other resource identifiers are already exposed in URLs.
* Whether refreshing or directly opening an existing URL reconstructs the corresponding state.
* How authentication redirects currently preserve or lose the original requested URL.
* Whether there is already a reusable generic deep-link/shareable-resource mechanism, or whether routing is currently module-specific.

Do not implement or change anything.

Return a concise summary of the current architecture, what already exists, what is missing, and the recommended integration point for making all modules and job-related states shareable by URL, without proposing module-specific implementations yet.
