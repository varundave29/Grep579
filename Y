Analyze the Artifactory provisioning data for application 55042. Do not implement any changes yet. This is a discovery/analysis task only.

Context:
- Terraform/Azure Pipelines generate a new provisioning state file on every pipeline run.
- JSON and CSV contain the same data. Use JSON as the primary format for analysis.
- Multiple timestamped JSON files exist under the Artifactory JSON directory.
- A newer file may contain changes made by a later provisioning run.
- The eventual goal is to build a Utility Watch job that downloads these files, determines the latest authoritative VM/environment state, and displays the normalized procurement data.
- The screenshots provided show the Artifactory file listing and an example JSON structure.

Analyze the actual files/data available in the repository/environment and produce a detailed findings report covering:

1. File/version ordering
   - Determine how provisioning files are uniquely identified and ordered.
   - Analyze the filename format, embedded timestamps, pipeline/build identifiers, and Artifactory last-modified information.
   - Determine the most reliable way for the future job to establish chronological order.
   - Identify any cases where filename ordering may be unreliable.

2. Latest-file completeness
   - Determine whether the newest provisioning JSON is a complete snapshot of the application's/environment's current state or only an incremental/change record.
   - Compare multiple historical files, not just one.
   - Check whether VMs/resources that existed in older files continue to appear in the newest file.
   - Check whether resources that were removed disappear from the latest file or are represented with an explicit deleted/state indicator.
   - Determine whether the latest file alone can be used to reconstruct the current VM inventory.
   - Do not assume this; provide evidence from the actual data.

3. Historical comparison
   - Compare representative consecutive provisioning files and identify:
     - VMs added
     - VMs removed
     - VMs modified
     - VM properties that changed
     - unchanged VMs
   - Determine whether a VM can safely be identified across files using its name/host_name or another stable identifier.
   - Identify the best stable identity key for reconciliation.

4. JSON structure and data model
   - Map the complete JSON hierarchy and explain what each major section represents.
   - Identify all fields relevant to environment, application/product, procurement, subscription, compute configuration, VM configuration, networking, and state.
   - Identify nested arrays/objects and any relationships between them.
   - Identify fields that are optional, nullable, repeated, or inconsistent between files.
   - Identify fields that should eventually be exposed in Utility Watch versus fields that are primarily technical/internal metadata.

5. Subscription analysis
   - Analyze subscription_id across all available files.
   - Determine whether one application/environment can contain multiple subscription IDs.
   - Determine whether subscription_id changes over time.
   - Determine whether subscription_id should be part of the unique identity/key for an environment, VM, or procurement record.
   - Identify any other fields that appear to define the application/environment boundary.

6. VM state and lifecycle
   - Identify all fields that indicate VM state, lifecycle, deletion, modification, provisioning, or availability.
   - Determine how additions, deletions, and modifications are represented.
   - Check whether historical files provide enough information to reconstruct lifecycle history.
   - Determine whether the latest file already represents the final lifecycle state.

7. Cross-file consistency
   - Check whether JSON files follow a consistent schema.
   - Identify schema changes over time.
   - Check whether the same information is represented differently in older and newer files.
   - Check whether duplicate VM records occur within a single file or only across different files.

8. JSON versus CSV
   - Since JSON and CSV are expected to contain identical information, verify this using representative files.
   - Determine whether there is any meaningful difference in fields, records, structure, or completeness.
   - Recommend whether the future Utility Watch ingestion should use JSON only, CSV only, or retain both as validation/fallback sources.
   - Prefer the format that gives the cleanest and safest implementation, not simply the easiest format to parse.

9. Proposed reconciliation model
   Based strictly on the findings, describe how the eventual job should work:
   - discover files
   - determine ordering
   - download required files
   - parse them
   - identify the application/environment
   - identify subscriptions
   - identify VMs
   - determine current/latest state
   - handle additions, modifications, and deletions
   - deduplicate records
   - produce the final normalized dataset for Utility Watch

10. Data model recommendation
   Propose the logical entities/records that Utility Watch should store/display. For example, determine whether the data should conceptually be separated into:
   - application/environment
   - subscription
   - product/classification
   - compute configuration
   - VM
   - VM networking
   - provisioning history

   Do not implement the model. Explain the recommended relationships and unique keys.

11. Edge cases and risks
   Identify anything that could make a simple "take the latest file" approach incorrect, including:
   - multiple subscriptions
   - multiple environments
   - concurrent pipeline runs
   - files arriving out of order
   - partial snapshots
   - deleted VMs
   - renamed VMs
   - duplicate records
   - schema changes
   - missing fields
   - stale/failed provisioning runs

12. Final conclusion
   Clearly answer these questions:
   A. Is the latest file a complete authoritative snapshot?
   B. Can Utility Watch derive the current VM inventory from only the latest file?
   C. If not, which historical files are required and why?
   D. What is the stable identity of a VM?
   E. What determines file ordering?
   F. What role does subscription_id play?
   G. What information should be persisted for the final Utility Watch view?
   H. What ingestion/reconciliation approach should be implemented next?

For every important conclusion, provide evidence from actual files rather than assumptions.

Do not modify application code, database schemas, jobs, or UI.
Do not implement the ingestion job yet.
Do not make assumptions where the available data is insufficient; explicitly mark those points as requiring additional sample files or validation.

Return the result as a concise but technically complete analysis report, with examples of actual records/fields where useful.
