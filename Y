We need to fix ADO connectivity for the Master Record job and Image Version job when running on the remote Linux VM.

Both work correctly locally but fail on the remote VM with:

HTTP 403
The requested operation is not allowed.
The conditional access policy defined by your Microsoft Entra administrator has failed.

I have a working PM2 ecosystem.config.cjs example from another application on the same type of VM. It configures:

NODE_EXTRA_CA_CERTS=
HTTPS_PROXY=http:
HTTP_PROXY=http:/
NO_PROXY=

The important point is that our failing ADO jobs are Python, not Node. The traceback shows Python 3.9 urllib.request and the ADO request is made from find_digest_ado.py. Therefore, do NOT simply copy NODE_EXTRA_CA_CERTS into the Python implementation.

Please inspect the existing Master Record and Image Version ADO request implementations first.

Goals:

1. Remote VM:
   - ADO requests must use the corporate HTTP/HTTPS proxy:
     HTTP_PROXY
     HTTPS_PROXY
   - The supplied internal CA/certificate configuration must be supported if required for TLS trust.
   - Determine the correct Python mechanism for the CA certificate rather than assuming NODE_EXTRA_CA_CERTS applies.
   - Preserve the existing Entra/PAT authentication logic.

2. Local development:
   - Continue working without the proxy.
   - Do not force local traffic through Zscaler.
   - Preserve the current working behavior.

3. Configuration:
   - Use environment variables rather than hardcoding proxy URLs, certificate paths, credentials, or tokens.
   - Do not modify the existing Pega package deployment certificate configuration.
   - Prefer dedicated ADO-related configuration if that keeps the configuration isolated from the existing Pega variables.

4. Python implementation:
   - Inspect how urllib.request currently creates the ADO request.
   - Verify whether the default urllib proxy handling will honor HTTP_PROXY/HTTPS_PROXY in this code path.
   - If explicit proxy handling is required, implement it cleanly.
   - Determine whether the supplied certificate is a CA bundle, server certificate, or something else before configuring it.
   - If Python needs an explicit CA configuration, use the appropriate mechanism such as SSL_CERT_FILE or an SSLContext only if actually required.
   - Do not add unnecessary TLS/proxy abstractions.

5. Apply the configuration consistently to BOTH:
   - Master Record job
   - Image Version job

6. Do not modify unrelated jobs.

7. Do not expose secrets, PATs, tokens, .env contents, or certificate contents in logs.

Before changing anything, identify:
- The exact Python files/functions making the ADO calls.
- The HTTP library/request mechanism being used.
- Whether urllib is already inheriting proxy environment variables.
- How environment variables are currently loaded.
- Where the remote VM process receives its environment.
- Whether the existing application launches these Python scripts as subprocesses and therefore passes the required environment variables to them.
- How the supplied internal certificate should be trusted by Python.

Important diagnostic distinction:

The current HTTP 403 is an Entra Conditional Access failure, not proof of an invalid PAT. Do not change authentication permissions/scopes or credentials unless investigation proves they are the problem.

The likely intended architecture is:

Remote VM
  -> corporate Zscaler proxy
  -> Azure DevOps
  -> Entra Conditional Access

Local:
  -> direct Azure DevOps connection

Implement the smallest change necessary.

After implementation, report:
- files changed
- environment variables required
- how local vs remote behavior is selected
- how the Python process receives HTTP_PROXY/HTTPS_PROXY
- how the internal CA certificate is trusted
- whether NO_PROXY is needed
- whether the existing Pega certificate configuration remains untouched
- what test should be performed on the VM to distinguish proxy/TLS connectivity from Entra authorization.

Do not refactor unrelated code.
