Implement a new generic Utility Watch job for Catalyst environment/provisioning metadata.

Use the completed 55042 analysis/implementation as the reference for the data model and expected output. Do not hardcode 55042-specific assumptions. The implementation must work for the current applications and future applications that follow the same metadata structure.

Applications:
- 55042
- <APPLICATION_ID_2>
- <APPLICATION_ID_3>
- <APPLICATION_ID_4>

Requirements:

1. Artifact discovery
- The Artifactory base URL/path is common up to the application/item ID.
- Under each application ID, JSON is the authoritative source. Ignore CSV completely.
- Standard structure contains a JSON directory.
- Some applications also contain a DB directory containing JSON files.
- If DB exists, inspect and process it according to the same metadata/reconciliation rules established during the 55042 analysis.
- Do not hardcode the number or names of environments. Discover them from the metadata.
- For each environment, determine the latest authoritative JSON artifact using the timestamp/version information established during the 55042 analysis.
- Download only the artifacts required to reconstruct the current state. Do not process the entire historical set unnecessarily.
- Re-evaluate the existing 55042 logic and make the artifact-selection logic generic for future applications.

2. Data processing
- Use JSON only.
- Preserve the hierarchical relationships in the JSON.
- Reuse the validated 55042 data interpretation and field mappings.
- Determine environment, subscription, VM identity, VM configuration, state, and other relevant metadata generically from the JSON structure.
- Do not hardcode environment names, VM names, subscription IDs, product names, or other application-specific values.
- If DB artifacts contribute additional VM/environment information, merge them correctly without creating duplicates.
- Produce the current/latest state only, based on the validated reconciliation logic.

3. Output tables

Generate two tables for each application:

Environment Summary
- Environment
- Subscription ID
- Total VM
- Total CPU
- Include the other fields established as part of the validated 55042 analysis if they are required.
- Aggregate the VM-level information correctly per environment.

VM Inventory
- Show the detailed VM-level breakdown using the fields established in the 55042 analysis.
- Use the label "VM Inventory", not "Combined VM Inventory".
- Ensure each VM appears once in the final current-state inventory.

4. Generic implementation
- Create a reusable Python processing module/script rather than application-specific logic.
- It must work for the current four applications and future applications following the same metadata contract.
- Configuration such as application IDs and Artifactory base paths should be data/config driven.
- Do not duplicate processing code for individual applications.
- Handle missing/optional fields and applications with or without the DB directory gracefully.
- Reuse existing project utilities, authentication, artifact download, job execution, database, and UI patterns wherever applicable.

5. New Utility Watch job
- Add a new job to the existing Console module.
- Use a clear job name such as "Catalyst Environment Inventory" unless the existing naming conventions suggest a better equivalent.
- The job operates at the application level. The user should not need to provide additional parameters when starting it.
- Follow the existing job/RBAC/application access model.
- The user clicks the job action to execute it.
- Use "Run" as the action label unless the existing Console UX has a more appropriate established convention.
- On Run, discover/download the latest required Artifactory artifacts, process them locally, and generate the two tables.
- Do not continuously poll Artifactory. Data should be fetched when the job is run.

6. UI
Display the results vertically in this order:

Environment Summary
[table]

VM Inventory
[table]

- Make the tables usable without making the entire page/table area unnecessarily horizontally or vertically scrollable.
- Follow the existing Console styling and table components.
- Handle larger VM inventories using the existing pagination/table patterns if appropriate.
- Preserve the existing loading/progress behavior used by other jobs where applicable.
- Show clear errors when artifact discovery, download, parsing, or reconciliation fails.

7. CSV download
- Add a Download CSV action using the same mechanism/functionality already implemented by the Image Version job.
- The generated CSV should contain both sections in one file.

Format:

Environment Summary

<environment summary table>

[one empty line]

VM Inventory

<VM inventory table>

- Use proper CSV escaping, headers, column ordering, and formatting.
- Make the exported file readable and presentation-friendly.
- Do not simply dump raw JSON into the CSV.
- Use the same download implementation/pattern already used elsewhere in the application.

8. Validation/testing
- Inspect the existing 55042 implementation/tests and reuse the established expected results.
- Add/update tests for the generic processor and job.
- Test at least:
  - multiple environments
  - multiple subscriptions where applicable
  - repeated historical artifacts
  - latest-artifact selection
  - applications with JSON only
  - applications with JSON + DB
  - duplicate VM records
  - missing/optional fields
  - CSV generation
  - application IDs other than 55042
- Do not break existing jobs or Console functionality.

Before implementing, inspect the existing 55042 analysis/code and the current Console job architecture. Use the existing implementation as the reference for the validated metadata mapping, but make the new solution generic rather than copying 55042-specific assumptions.

Implement the complete feature, update relevant tests, and provide a concise summary of the changes and validation performed.
