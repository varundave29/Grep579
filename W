We need to add active-page background refresh to Deployment Manager Overview.

First inspect the existing Deployment Manager routing, Overview component lifecycle, discovery API, namespace switching logic, and existing refresh behavior. Do not implement anything until you understand how the current flow works.

Requirement:

1. Overview must continue doing an immediate discovery whenever the user enters/returns to Overview or switches namespace.
2. While the Overview page/component is actively mounted, perform a background discovery every 30 seconds.
3. The polling must be client-side and scoped to the active Overview page/component. Do NOT create a global backend scheduler.
4. Do NOT use session IDs or database tracking to determine whether a user is viewing Overview. React/router component lifecycle should control the polling lifecycle.
5. When the user navigates away from Overview, immediately stop/clean up the polling timer.
6. If multiple users are logged into the application, only users who are actively viewing Deployment Manager → Overview should generate these 30-second refresh requests.
7. Pause polling when the browser tab/document is hidden, if this can be implemented cleanly with the existing architecture. When the user returns to the tab, trigger an immediate discovery and resume the 30-second cycle.
8. Do not visually reload the entire page on every refresh. Reconcile the latest deployment state into the existing UI without resetting scroll position, selections, animations, or unrelated state.
9. Do not show the full blue initial-discovery spinner for every background refresh. Background refresh should be visually subtle.
10. If a background refresh fails, preserve the last known state and expose an appropriate stale/error indicator rather than clearing the UI.
11. After a Start, Stop, Restart, or Scale action, trigger an immediate targeted refresh rather than waiting for the next 30-second interval.
12. Ensure timers/listeners are properly cleaned up to prevent memory leaks, duplicate polling, or multiple intervals after navigation/re-rendering.

Before coding, report:

* where the current Overview discovery is implemented
* how routing/navigation is handled
* whether there is already a polling/refresh mechanism
* which API endpoint performs discovery
* whether the discovery operation is expensive
* the exact component/effect where you propose to attach the lifecycle-controlled polling

Then implement the smallest clean change that fits the existing architecture. Do not introduce WebSockets, session tracking, database state, or a new global polling service unless the existing architecture makes the component-level approach impossible.
