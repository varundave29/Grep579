We have completed and validated the Image Version implementation.

The current Image Version functionality is working correctly across multiple applications. The ADO lookup, repository/build/branch/commit resolution, Kubernetes image discovery, and UI output have already been validated.

During troubleshooting, temporary diagnostic tracing was added to identify a session/restart issue. The issue was ultimately resolved by performing a complete clean restart of the local utility, which removed stale frontend/backend processes and started the current processes cleanly.

I now need ONLY two cleanup tasks.

Task 1: Remove temporary Image Version debugging

Inspect the Image Version code and remove the temporary diagnostic/tracing code that was added specifically during troubleshooting.

Remove debugging-only items such as:

* verbose [TRACE] logging
* per-step diagnostic logging
* temporary subprocess tracing
* temporary HTTP request tracing
* temporary loop/pagination tracing
* other investigation-only instrumentation

However, KEEP legitimate production functionality such as:

* HTTP request timeouts
* proper exception handling
* meaningful error handling
* normal application/job logging

Do NOT modify or refactor the working Image Version logic.

Do NOT change:

* Kubernetes image discovery
* repository extraction
* digest extraction
* ADO REST API logic
* repository/build/branch/commit matching
* JSON output
* Image Version UI
* application/session selection logic

The objective is simply to remove temporary debugging code while preserving the exact working behavior.

After cleanup, verify that Image Version still works.

Task 2: Synchronize the restart script with the successful clean restart procedure

There is an existing local restart script used to start/stop the utility.

During the Image Version troubleshooting, the issue was ultimately resolved by completely stopping the stale frontend/backend processes and then starting the current processes cleanly.

Inspect:

1. The existing restart script.
2. The current frontend/backend startup configuration.
3. The processes and ports used by this utility.
4. The actual clean restart procedure that is currently required.

Update the existing restart script so that it reliably performs the same clean restart procedure that successfully fixed the issue.

The restart script should:

1. Identify only the utility’s relevant frontend/backend processes.
2. Stop those existing processes cleanly.
3. Verify the relevant ports are no longer occupied.
4. Start the intended current frontend/backend processes.
5. Verify that the expected processes are running afterward.
6. Preserve the existing development/hot-reload setup.
7. Preserve the existing backend startup configuration.

Do NOT blindly kill unrelated processes.

Do NOT hardcode temporary PIDs.

Do NOT introduce a new process-management architecture.

Do NOT change application functionality.

The goal is simply to make the existing restart script reliably reproduce the clean restart that is already known to work.

Important constraints

This is a cleanup and maintenance task.

Do NOT:

* redesign Image Version
* refactor unrelated code
* change authentication/session architecture
* change Kubernetes logic
* change ADO logic
* change the UI
* change database behavior
* add new features

Before changing the restart script, understand the current implementation and determine exactly what the successful clean restart did differently.

If anything is ambiguous, inspect the existing code/configuration rather than guessing.

Validation

After both tasks:

1. Run the updated restart script.
2. Confirm the frontend starts correctly.
3. Confirm the backend starts correctly.
4. Confirm no stale utility processes remain.
5. Confirm the expected ports are owned by the current utility processes.
6. Confirm Image Version still works for multiple applications.
7. Confirm the temporary diagnostic [TRACE] output is gone.
8. Confirm no unrelated processes were stopped or modified.

Keep the changes minimal and limited to these two tasks.
